spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/products
  application:
    name: product-service
server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
